"""Checks that turn this package's promises into properties of its source.

Both run in CI *and* at peer startup, so each is a claim about the running program rather than
about a review that once happened.
"""
